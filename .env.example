EXPO_PUBLIC_API_BASE_URL=https://simhastha360-backend.vercel.app
# For a local backend on the same Wi-Fi instead, use your machine's LAN IP,
# e.g. http://192.168.1.2:8000 (127.0.0.1 won't reach your PC from a phone/emulator)

# Static key from a "Web" application in the Mappls console (Application ->
# Credentials). Used client-side to load the map and call REST APIs.
EXPO_PUBLIC_MAPPLS_KEY=
